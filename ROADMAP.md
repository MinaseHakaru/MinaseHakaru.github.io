# サイト構想・ブランド戦略メモ

このファイルは事業判断・企画メモであり、`CLAUDE.md`（リポジトリでの技術的な作業指針）とは切り離して管理する。コードの実装方針そのものではなく、「何を作るか」「誰のために作るか」の検討記録。

## 0. このサイトは何のためにあるか（結論）

**主目的は (b)：『経済の構造を読む』の読者に向けた付属物。副次効果として (c) のSEOが乗る。この順序は逆ではない。**

| 位置づけ | 内容 |
|---|---|
| 主目的 (b) | 『経済の構造を読む』の読者に向けた付属物 |
| 副次効果 (c) | SEOによる新規読者の獲得 |

数字ツール群（実質値・複利・現在価値）は、水瀬衡が書いている内容そのものの実演になる。「記事で説明した計算を、読者がその場で自分の数字で試せる」構造は他に真似できない独自性になり、これが (b) を主目的に置く理由になる。SEOはこの独自性が結果として引き寄せるものであり、SEOを起点にURL設計やツール選定を決めることはしない。先に集客装置としての設計（キーワード網羅・URL構成）を組み、あとから (b) の動機を当てはめる、という順序は取らない。

(a)「純粋に自分用の道具」という動機も残るが、公開範囲やブランド戦略を左右する主軸には置かない。文字数カウントや単位換算のような「誰が作っても同じもの」は、(b) の文脈では優先度が低い。

## 1. 判断原則：これは測る道具か

新機能を追加するかどうかは、最終的にこの問いひとつで判断する。**「これは測る道具か」。**

文章の長さ、漢字の難度、数字の差、実質価値、時間の距離 —— 一見バラバラなこれらは、すべて「測る」という一つの営みに回収できる。何かと何かの間にある、見えにくい関係を測って見通しよくすること。これがこのサイトの一貫した仕事であり、新機能の採否はこのゲートで判断する。

このゲートに照らすと、次の機能案は不採用になる：
- **四則演算電卓** — ただの計算であり、何かを測ってはいない。
- **単位換算** — 異なる尺度を置き換えるだけで、関係を測って意味を明らかにするものではない（§5）。
- **為替表示** — レートをそのまま表示するだけで、測る行為が発生しない。しかも外部通信が必要になり、「入力を外に出さない」というサイトの核と衝突する。

## 2. コアコンセプト

**書く・測る・比べる・読み解くための、静かで実用的な道具。**

水瀬衡名義で展開する上での核。フッターに明示している3原則（インストール不要／ブラウザだけで動作／入力内容を外部送信しない）は、文章の機密性を気にする層に対して一般的なAI文章サービスと違う立ち位置を作れる、実質的な差別化要素になっている。

サイト全体の短い説明文の方向性（案）：
- 「文章、時間、数字を、少し見通しよくするための小さな道具集。」
- 「水瀬衡がつくる、書く・測る・比べるための道具帖。」

## 3. サイト全体のカテゴリ構想

| 分類 | 役割 | 代表ツール |
|---|---|---|
| 文章を整える | 書く・読む・伝える | 現在の5機能 |
| 時間を見渡す | 離れた場所や期間を比べる | 時差、期間、営業日 |
| 数字を読み解く | 割合や変化の意味を理解する | 割合計算、実質値、複利 |
| ~~単位を換える~~ | ~~異なる尺度を行き来する~~ | ~~通貨・長さ・容量・温度~~ |

「単位を換える」は §5 の理由により見送り推奨。「文章」「時間」「数字」の3カテゴリは、いずれも「見えにくい関係を測って分かりやすくする」という一点で統一できる。

## 4. 推奨する着手順序

### 1〜5：公開可能な製品を作る

1. 「要約」を外す。
2. `index.html` にリネームし、Public化してGitHub Pagesで公開する。
3. 実機確認と3〜5人への個別配布を行う（4と並行して進める）。
   - 実機確認で見つかり対応済み：段落先頭の二重字下げ、会話文行頭の不要な字下げ、全ツールへのリセットボタン追加、原稿用紙の説明文に横書きを明記。
   - 実機確認で見つかり未対応（5番目で対応予定）：原稿用紙エディタのページャ改善（枚数が多い場合に目的のページへ到達できない）。
4. 割合・増減率を実装する。**完了。**
   - 二つの数の関係を、差・増減率・倍率（％指定時はポイント差）で測り、文章化するツールを実装した。名称は「数くらべ」。
   - 境界条件としてゼロ・負数・符号跨ぎを明示的に処理し、誤読を招く尺度は数値を出さず理由を明示する設計とした。
   - 「これは測る道具か」（§1）というゲートを、新規ツールの仕様決定から実装まで初めて通して適用した工程である。逆算機能・分数表現は同ゲートにより初版から除外した。
   - 「入力 → 測る → 言葉にする」という構造を持たせ、文章ツール群と数字ツール群の接続点とした。
   - 次は5番目（原稿用紙エディタの積み残し：縦書き対応・ページャ改善）に進む。
5. 原稿用紙エディタの積み残しに対応する（縦書き対応・ページャ改善）。**完了。**
   - **縦書き対応**：20字×20列、右上から下へ、列が終わったら左へ。横書きは廃止し、切り替え機能は設けていない。
   - マスサイズは最小20pxを下限とするレスポンシブ。20px未満には縮めず、400px未満のviewportでは横スクロールする。実機で15/20/24/28pxを比較し、可読性と全体像の保持の境界として20pxを選定した。
   - 初期表示は用紙の右端（第1列）。ページ切替時も右端に戻す。
   - 約物（読点・句点・小書き仮名・長音符・ダッシュ・かっこ類・？！）は縦書き字形・位置を仕様として固定し、実現方法は実装側に委ねた。
   - 印刷はA4縦・1ページ1枚。
   - **ページャ改善**：先頭・最終へのジャンプ、ページ番号の直接入力、左右矢印キーでの移動、記号ボタンへのaria-label付与を実装した。
   - **モバイルヘッダー暫定修正**（工程5の途中で実施）：ナビが2段折り返しでヘッダーが151pxまで肥大化していた問題に対し、モバイル幅でナビを1行横スクロールに変更（104.7pxに縮小）。ブランド階層・カテゴリ分けには触れておらず、工程6での正式な再設計を前提とした暫定措置である。6ツールの時点でナビが2段化した事実は、工程6でナビ構造を再設計する根拠となる。
   - 次は6番目（上位ブランド「水瀬衡の道具帖（仮）」へのUI再編）に進む。

### 6〜7：サイトを設計する

6. 上位ブランド「水瀬衡の道具帖（仮）」へUI再編する。**完了。**
   - **上位名称は「道具帖」。** 作者名は前面に出さず、フッターから水瀬衡へ接続する構造にした。検索から来た利用者に、利用に不要な固有名詞を最初に処理させないため。道具の設計思想から作者へ逆流させる構造を選んだ（「水瀬衡の道具帖（仮）」という当初案は不採用）。
   - **「文章づくり道具帖」は縮めず、文章カテゴリの名称として維持した。** 上位名称を成立させるために、すでに完成している下位名称を弱めるのは設計順序が逆であるため。
   - **「ものさし」は名称ではなく、説明文と設計思想として保持した。** 「測る」（§1）は機能選定の原則としては強いが、全角⇔半角やかな⇔ローマ字のような「整える」道具まで含む表面名称としては狭いため、サイト名やカテゴリ名には採用しなかった。フッターの一文（「水瀬衡が…小さな『ものさし』をもとにつくっています。」）に留めている。
   - **情報構造は文章／数字／時間の3カテゴリで確定した。** ただしカテゴリUIは出さず、トップページに非操作の見出しを置くのみ（`.cover-group-heading`、リンク化・タブ化・折り畳み化はしない）。「時間を見渡す」は公開ツールがないため、DOMにも出力していない。
   - **ヘッダーナビは工程5の1行横スクロール方式を維持した。** 6ツール程度であれば、カテゴリを挟まず直接ツール名が見えるほうが到達が速く、カテゴリを挟むと操作が一段増えるため。工程5で「暫定修正」としていたこの方式は、ツール数が少ない現段階では単に合理的な方式として評価し直した。
   - **カテゴリUIの導入条件：** 以下のいずれか早い時点で、本格的なカテゴリUI（ナビの階層化・カテゴリページ）へ移行する。
     1. 横スクロールナビで、端のツールへの到達に複数回のスワイプが必要になる。
     2. 文章・数字・時間の3カテゴリすべてに公開ツールが存在する。
   - **「情報構造」と「UIへの露出」を分離する方針を採った。** カテゴリUI（ナビの階層化・カテゴリページ）への露出は、上記の導入条件が満たされる＝需要が発生するまで遅らせる。**URL構造はこの分離の対象外で、確定した3カテゴリの情報構造にそのまま従う。**（当初この一文は「URL構造・ナビのカテゴリ化という『露出』は遅らせる」と書かれていたが、直後の7番目にある「URLは3カテゴリ構造に従う」と矛盾していたため、工程7の着手時に修正した。理由は可逆性の違いである。ナビは平置き↔カテゴリを何度でも往復できるが、URLを後から `/tools/character-count/` → `/tools/writing/character-count/` へ変えると、リダイレクト・外部リンク・検索インデックス・ブックマークを抱え込む。したがって情報構造は今URLに固定し、それをナビとして見せるのは必要になるまで待つ。）
   - 次は7番目（実URL構成への移行）に進む。URLは本工程で確定した文章／数字／時間の3カテゴリ構造に従う。ただし「時間を見渡す」はツールがまだ存在しないため、空の `/tools/time/` を先回りして作る必要はない（ツールが追加された時点でカテゴリごと作る）。
7. 実URL構成へ移行する。**完了。**
   - **単一HTML＋ハッシュルーター → 複数ページ＋実URL** へ移行した。工程6で決めた情報構造を、カテゴリ階層を持つ実URLへ固定した工程である。

     ```
     /index.html
     /404.html
     /assets/style.css
     /assets/common.js
     /tools/writing/character-count/index.html
     /tools/writing/width-converter/index.html
     /tools/writing/kana-romaji/index.html
     /tools/writing/manuscript-paper/index.html
     /tools/writing/kanji-check/index.html
     /tools/numbers/number-comparison/index.html
     ```

   - **URLにカテゴリ階層を含めた。** 判断根拠は §4 6番目に追記したとおり、URLとUIでは可逆性が違うため。`/tools/writing/` と `/tools/numbers/` は現時点でページを持たず404になるが、これは許容する。親ディレクトリにページが存在しなければならない決まりはなく、空のカテゴリページを先回りして作るほうが「空の器を見せない」という工程6の判断を曖昧にする。
   - **スラッグは今回リセットした。** 実URL化は名前を付け直せる唯一の機会であるため、旧ハッシュの名残を引きずらせなかった。特に `ratio`（旧名「割合・増減率」の名残）は現在の「数くらべ」の仕様を表していないため捨て、`number-comparison` を採った。「二つの数を比較する」という機能本体に紐づくため、表示名が将来多少変わってもずれにくい。
   - **共通化は「CSS＋共通JS」の境界で止めた。** `style.css`（見た目）と `common.js`（複数ページが実際に共有する基礎ロジック＝toast・copyText・fmtNum・escapeHtml・layoutGenkou）だけを共通ファイルにし、各ツールの計算・変換ロジックはそのツールのページに残した。CSSだけを共通化すると `layoutGenkou()` が文字数カウントと原稿用紙エディタで二重化し、禁則や原稿用紙仕様を直すたびに同期漏れの危険が出る。逆に全JSをモジュールへ分解するのは6ツール規模では過剰である。
   - **旧ハッシュURLは期限を設けずに残した。** `index.html` に6件の互換マップを置き、`#/count` 等を新URLへ `location.replace()` で転送する。維持コストがほぼゼロである以上、壊す理由がない。
   - **本文の書き下ろしは行わなかった。** 工程7の範囲はファイル分割・共通アセット・実URL・リンク書き換え・旧ハッシュ互換・ページ固有のtitle/description・404までとし、使い方・計算根拠・注意事項の新規執筆は含めていない。工程4・5と同じく変更軸を混ぜないためである。サイト基盤の変更と本文追加を同時に行うと、移行後に表示が変わったとき「構造変更による問題か、新しい説明文による問題か」が切り分けられなくなる。§10 が挙げる「使い方・計算根拠・注意事項を置ける」は、実URL化によって**得られた能力**として残す。必要なページから随時充実させればよく、そのための工程を別立てはしない。特に工程8の実質値はnote記事との相互リンクを含むため、そこで初めて「計算根拠まで持った数字ページ」の型を作れる。
   - 回帰確認は、ローカル静的サーバ上でページのJS状態を直接叩く方式（CLAUDE.md 参照）で87項目を実施した。全ページの遷移・title・description・ナビの現在地表示、6ツールの動作、旧ハッシュ6件の転送、モバイル幅のヘッダー、`/tools/` 系にindexを置いていないことを確認済み。
   - 次は8番目（実質値の実装とnote記事）に進む。

### 8〜9：水瀬衡ブランドと接続する

8. 実質値を実装し、それを説明するnote記事を1本書く。
9. 正式告知する。
   - **正式告知前チェック**
     - favicon がサイトに存在しない。ブラウザのタブが白紙アイコンになるため、正式告知の前に用意する。（工程7の実機確認で判明。工程7の合意範囲外だったため対応を見送った）

### 10：継続運営

10. 複利 → 現在価値 → 時差の順に追加していく。

## 5. 見送り推奨の機能案

### 単位換算（非推奨）

- 長さ・温度などはコモディティで、独自に作る理由がない。
- 通貨換算は為替レート取得が必要 = 外部通信が発生する。「入力を外に出さない」というサイトの核の価値と正面から衝突する。この一機能のために、サイト全体の説明文（「入力文章は送信されません」）を弱めることになる。

## 6. 時差ツール：作るなら方式を限定する

サマータイムの規則は国単位で毎年のように変わりうる（例：メキシコは廃止、欧州は議論継続）。自前でオフセット表を持つと、静かに間違ったサイトになるリスクがある。

**実装するなら `Intl.DateTimeFormat` に IANA タイムゾーン名を渡し、オフセット計算はブラウザ側の最新データに完全に委ねる方式に限定する。** 自前のタイムゾーン/DSTテーブルは持たない。この方式で作れない機能（＝ブラウザの標準APIでは表現できない要件）は作らない、と決めておく。

機能案（優先度は上から）：
- 複数都市の現在時刻を横並び表示
- 基準日時を変更して将来・過去の時差を計算
- 夏時間の自動反映（`Intl.DateTimeFormat` 任せ）
- 日付が前日・翌日になる場合の明示
- 2都市の営業時間が重なる帯を色分け表示（★独自性が出るポイント。通常の勤務時間／早朝・夜間／両者に無理の少ない時間、を帯で見せる）
- 「東京の午前9時は各都市で何時か」の一覧
- 結果を文章としてコピー
- URLで都市と日時を共有

名称は「時差計算」よりも「世界の時間を重ねる」「時刻くらべ」のような道具帖らしい名称を併記できる。

## 7. Web電卓：拡張案（優先度順）

一般的な四則演算電卓だけでは個性が出ないため、基本電卓と「意味を読む計算」を分ける。

1. **割合・増減率**（最優先）：「120から150は何％増か」「20％引きの元値はいくらか」
2. **複利・積立**：元本・利率・期間・毎月積立額から将来額を計算し、元本と運用益を分けて表示
3. **名目値と実質値**：物価上昇を差し引いた実質的な増減を示す。水瀬衡名義との結びつきが特に強い機能
4. **現在価値・将来価値**：将来のお金を現在の価値に直す。「なぜ現在価値が下がるのか」を短く説明できるとよい
5. **年率・月率の換算**：単純に12で割る方式と複利換算の違いを並べると教育的価値が出る
6. **比率を文章に直す**：「前年比7.5％増」「1.4倍」「全体の約3分の1」など、計算結果を文章表現としてコピーできる機能。文章ツール群との接続点にもなる

## 8. 既存6機能の発展余地

### かな・ローマ字変換
「簡易変換」としては十分だが、以下は表現上の限界として残る：
- 「ん」の後に母音・yが続く場合の曖昧さ
- 外来語の「ティ」「ファ」「ヴァ」など拡張カタカナ
- 長音を ō／ou／oo のどれで表すか
- ヘボン式・訓令式の切り替え
- 大文字や固有名詞の扱い

### 原稿用紙エディタ
会話文・行末禁則の強化・段落がページをまたぐ場合の処理などを段階的に強化できる。（縦書き対応は「発展余地」ではなく未達成の実装項目のため、§4 推奨する着手順序の5番目で扱う）

**積み残し（発展余地ではなく不具合）：約物の見え方。** 工程7の実機確認で、句読点（、。）と小書き仮名（っゃゅょ等）が小さすぎ、罫線に貼り付いて読みにくいことが分かった。工程5で入った表示であり、工程7では該当CSSを一字も触れていない。

- 原因：`.gk-cell.gk-corner .glyph` が句読点と小書き仮名を同一クラスで扱い、両方に `font-size: 0.6em` と `inset: 4% 4%` を二重にかけている。原稿用紙の慣例では、句読点は縮小せず右上の区画に置き、小書き仮名は縮小するものの角には貼り付けない。
- 候補案B：`gk-punct` と `gk-small` にクラスを分割し、句読点はフォントの縦書き字形に委ね（原寸）、小書き仮名は原寸のまま右上へ寄せる。慣例に最も近い見た目になるが、Shippori Mincho の縦書き字形（vert機能）に依存するため実機確認が要る。開きかっこ類を既定の縦書き字形に委ねているのと同じ考え方（CLAUDE.md 参照）。
- 候補案C：現方式のまま `0.85em` ・ `inset: 12% 10%` に緩める。フォントの機能に依存せず確実に効くが、「縮小して角へ寄せる」という方式自体は残る。
- 工程7（実URL移行）とは変更軸が違うため、工程7には含めず別に扱う。案Bを入れて実機で崩れたら案Cへ落とす、という順序が取れる。

### 漢字利用チェック
学年判定だけでなく、以下まで広げると独自性のある機能になる：
- 漢字ごとの出現回数
- 対象外漢字の一覧
- 指定学年までに収めるための言い換え候補
- ふりがなが必要そうな語の抽出
- 常用漢字・人名用漢字・表外字の区分

教育・児童向け文章、読みやすさ確認の文脈に発展させやすい。

### 要約（重要文抽出）→ 廃止決定
「要約」はサイトから外すことを決定した（§4 推奨する着手順序の1番目）。理由：
- 「要約」という名称が「AIによる意味的要約ではない」という実態とずれていた。
- 頻度ベースの抽出は日本語では精度が出にくく、「がっかりされる機能」を一つ抱えるコストが、機能を一つ増やす利益より大きいと判断した。

## 9. UI・アクセシビリティの将来課題

- モバイルでは現状、上部に全ツール名が並ぶ設計になっている。ツールが増えるとナビゲーションが破綻するため、将来的には「上部は文章・時間・数字などの大分類、個別ツールはカテゴリ内のカード、モバイルはカテゴリメニュー」という階層構造に変更する。
- ダークテーマのCSSは用意されているが、手動切替ボタンはまだない（現状はOS設定に追従するのみ）。手動切替と設定保存があると完成度が上がる。
- コピー・リセットのアイコン補助、処理結果の読み上げ通知、見出し構造などの改善余地あり。
- 入力内容をlocalStorageに保存し、再訪問時に復元できるようにする案がある。利便性のための機能であり、§1の「これは測る道具か」の判断原則には直接かからない（測る行為そのものではなく、入力の手間を減らす付随機能のため）。優先度は原稿用紙エディタの縦書き対応（§4 推奨する着手順序の5番目、積み残し）より下に置く。

## 10. サイト構成（URL・保守性）— 工程7で実施済み

ハッシュ形式（`#/count` 等）では、各ツールが実質的に同じHTMLとして扱われ、検索エンジンからは1ページとしてしか見えなかった。工程7でこれを解消し、各ツールが実URLを持つ構成へ移行した（構成は §4 7番目を参照）。

移行にあたって、ビルドツールもフレームワークも導入していない。素のHTMLファイルを複数に分け、CSSと共有ロジックだけを `/assets/` に切り出す形で、「ファイルを置くだけ」という単純さは保たれている。**フレームワーク導入はツールが15個を超えてから検討すれば十分。**

実URL化によって、各ページに固有のタイトル・説明・使い方・計算根拠・注意事項・関連記事への導線を置ける**ようになった**。ただし工程7ではtitleとmeta descriptionまでしか行っていない。本文の充実は、必要になったページから随時進める（§4 7番目）。

保守上の残コストとして、ヘッダーナビ・フッターが全8ファイルに重複している。ツール追加時は全ページのナビを揃えて更新する必要がある（CLAUDE.md に手順あり）。ビルドを持たない構成の代償として、この重複は現時点では受け入れている。テンプレート化を検討する目安も、上記のツール15個と同じでよい。
