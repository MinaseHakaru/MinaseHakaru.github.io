# サイト構想・ブランド戦略メモ

このファイルは事業判断・企画メモであり、`CLAUDE.md`（リポジトリでの技術的な作業指針）とは切り離して管理する。コードの実装方針そのものではなく、「何を作るか」「誰のために作るか」の検討記録。

## 0. まず決めるべき問い：このサイトは何のためにあるか

検索流入やURL設計の話をする前に、立ち位置を決める必要がある。考えられる選択肢は3つ。

| 選択肢 | 内容 |
|---|---|
| (a) | 純粋に自分用の道具で、公開はついで |
| (b) | 『経済の構造を読む』の読者に向けた付属物 |
| (c) | 独立した集客装置（SEOで新規読者を得る） |

**現状の構想は (c) の設計（URL設計・SEO言及）をしながら、動機は (a) か (b) に寄っている。** ここが定まらないと、SEOにどれだけ投資すべきか、ツールを何個作るべきかが決まらない。

**推奨は (b)。** 理由：数字ツール群（実質値・複利・現在価値）は、水瀬衡が書いている内容そのものの実演になる。「記事で説明した計算を、読者がその場で自分の数字で試せる」構造は他に真似できない独自性になる。逆に文字数カウントや単位換算は、誰が作っても同じものであり、(b) の文脈では優先度が低い。

## 1. コアコンセプト

**書く・測る・比べる・読み解くための、静かで実用的な道具。**

水瀬衡名義で展開する上での核。フッターに明示している3原則（インストール不要／ブラウザだけで動作／入力内容を外部送信しない）は、文章の機密性を気にする層に対して一般的なAI文章サービスと違う立ち位置を作れる、実質的な差別化要素になっている。

サイト全体の短い説明文の方向性（案）：
- 「文章、時間、数字を、少し見通しよくするための小さな道具集。」
- 「水瀬衡がつくる、書く・測る・比べるための道具帖。」

## 2. サイト全体のカテゴリ構想

| 分類 | 役割 | 代表ツール |
|---|---|---|
| 文章を整える | 書く・読む・伝える | 現在の6機能 |
| 時間を見渡す | 離れた場所や期間を比べる | 時差、期間、営業日 |
| 数字を読み解く | 割合や変化の意味を理解する | 割合計算、実質値、複利 |
| ~~単位を換える~~ | ~~異なる尺度を行き来する~~ | ~~通貨・長さ・容量・温度~~ |

「単位を換える」は §4 の理由により見送り推奨。「文章」「時間」「数字」の3カテゴリは、いずれも「見えにくい関係を測って分かりやすくする」という一点で統一できる。

## 3. 推奨する着手順序

1. **既存6機能のバグ修正・表記整理を最優先で行う。** 今あるものが正確でないまま機能を足すのは負債を増やす方向。技術的な既知の不整合は `CLAUDE.md` の「Known inconsistencies」に記録済み。
2. サイト全体の総称・カテゴリ構成を決める（§0 の問いに答えを出す）。
3. 「割合・増減率計算」を追加する。実装が小さく、需要があり、本業（経済の説明）に直結する。
4. 複数都市対応の時差・会議時間ツールを追加する（§5 の制約を守れる場合のみ）。
5. 実質値・複利など、水瀬衡らしい数字ツールを加える。
6. ハッシュルーティングの単一HTMLから、実URLを持つ複数ページ構成へ移行する。

※ 移行（6番目）は最後に置きすぎない方がよい。ハッシュ形式のまま被リンクや検索インデックスが付いてから実URLに移すと、リダイレクト設計という余計な手間が発生する。§6 の中間案を参照。

## 4. 見送り推奨の機能案

### 単位換算（非推奨）

- 長さ・温度などはコモディティで、独自に作る理由がない。
- 通貨換算は為替レート取得が必要 = 外部通信が発生する。「入力を外に出さない」というサイトの核の価値と正面から衝突する。この一機能のために、サイト全体の説明文（「入力文章は送信されません」）を弱めることになる。

## 5. 時差ツール：作るなら方式を限定する

サマータイムの規則は国単位で毎年のように変わりうる（例：メキシコは廃止、欧州は議論継続）。自前でオフセット表を持つと、静かに間違ったサイトになるリスクがある。

**実装するなら `Intl.DateTimeFormat` に IANA タイムゾーン名を渡し、オフセット計算はブラウザ側の最新データに完全に委ねる方式に限定する。** 自前のタイムゾーン/DSTテーブルは持たない。この方式で作れない機能（＝ブラウザの標準APIでは表現できない要件）は作らない、と決めておく。

機能案（優先度は上から）：
- 複数都市の現在時刻を横並び表示
- 基準日時を変更して将来・過去の時差を計算
- 夏時間の自動反映（`Intl.DateTimeFormat` 任せ）
- 日付が前日・翌日になる場合の明示
- 2都市の営業時間が重なる帯を色分け表示（★独自性が出るポイント。通常の勤務時間／早朝・夜間／両者に無理の少ない時間、を帯で見せる）
- 「東京の午前9時は各都市で何時か」の一覧
- 結果を文章としてコピー
- URLで都市と日時を共有

名称は「時差計算」よりも「世界の時間を重ねる」「時刻くらべ」のような道具帖らしい名称を併記できる。

## 6. Web電卓：拡張案（優先度順）

一般的な四則演算電卓だけでは個性が出ないため、基本電卓と「意味を読む計算」を分ける。

1. **割合・増減率**（最優先）：「120から150は何％増か」「20％引きの元値はいくらか」
2. **複利・積立**：元本・利率・期間・毎月積立額から将来額を計算し、元本と運用益を分けて表示
3. **名目値と実質値**：物価上昇を差し引いた実質的な増減を示す。水瀬衡名義との結びつきが特に強い機能
4. **現在価値・将来価値**：将来のお金を現在の価値に直す。「なぜ現在価値が下がるのか」を短く説明できるとよい
5. **年率・月率の換算**：単純に12で割る方式と複利換算の違いを並べると教育的価値が出る
6. **比率を文章に直す**：「前年比7.5％増」「1.4倍」「全体の約3分の1」など、計算結果を文章表現としてコピーできる機能。文章ツール群との接続点にもなる

## 7. 既存6機能の発展余地

### かな・ローマ字変換
「簡易変換」としては十分だが、以下は表現上の限界として残る：
- 「ん」の後に母音・yが続く場合の曖昧さ
- 外来語の「ティ」「ファ」「ヴァ」など拡張カタカナ
- 長音を ō／ou／oo のどれで表すか
- ヘボン式・訓令式の切り替え
- 大文字や固有名詞の扱い

### 原稿用紙エディタ
現状は横書きの簡易版。本格的な原稿用紙を期待する利用者向けには、縦書き・会話文・行末禁則の強化・段落がページをまたぐ場合の処理などを段階的に強化できる。

### 漢字利用チェック
学年判定だけでなく、以下まで広げると独自性のある機能になる：
- 漢字ごとの出現回数
- 対象外漢字の一覧
- 指定学年までに収めるための言い換え候補
- ふりがなが必要そうな語の抽出
- 常用漢字・人名用漢字・表外字の区分

教育・児童向け文章、読みやすさ確認の文脈に発展させやすい。

### 要約（重要文抽出）
- 「要約」という名称は「AIによる意味的要約ではない」という実態とずれる。**「重要文抽出」または「簡易抜粋」への改名を推奨。**
- さらに踏み込むなら、機能自体を残す価値があるかも再考の余地がある。頻度ベースの抽出は日本語では精度が出にくく、「がっかりされる機能」を一つ抱えるコストは、機能が一つ増える利益より大きい場合がある。

## 8. UI・アクセシビリティの将来課題

- モバイルでは現状、上部に全ツール名が並ぶ設計になっている。ツールが増えるとナビゲーションが破綻するため、将来的には「上部は文章・時間・数字などの大分類、個別ツールはカテゴリ内のカード、モバイルはカテゴリメニュー」という階層構造に変更する。
- ダークテーマのCSSは用意されているが、手動切替ボタンはまだない（現状はOS設定に追従するのみ）。手動切替と設定保存があると完成度が上がる。
- コピー・リセットのアイコン補助、処理結果の読み上げ通知、見出し構造、入力欄とラベルの明示的な関連付け（`<label for>`）などの改善余地あり。

## 9. サイト構成の将来設計（URL・保守性）

現在のハッシュ形式（`#/count` 等）では、各ツールが実質的に同じHTMLとして扱われ、検索エンジンからは1ページとしてしか見えない。公開サイトとして検索流入を狙うなら、最終的には実URLを持たせる方が有利：

```
/tools/writing/character-count/
/tools/writing/width-converter/
/tools/writing/kanji-grade/
/tools/time/timezone/
/tools/numbers/percentage-change/
```

各ページに固有のタイトル・説明・使い方・計算根拠・注意事項・関連記事への導線を置ける。

**移行は大掛かりに考える必要はない。** ビルドツールもフレームワークも導入せず、素のHTMLファイルを複数に分け、CSSだけ共通ファイルに切り出す中間案で十分：

```
/index.html
/assets/style.css
/tools/moji-count/index.html
/tools/kanji-grade/index.html
```

これで実URLが手に入り、「ファイルを置くだけ」という単純さも保たれる。**フレームワーク導入はツールが15個を超えてから検討すれば十分。**

単一HTMLは試作と配布には非常に強い一方、機能・データ・説明文が増えると保守しにくくなる。次の数機能までは現状方式（`writing_tools.html` 一本）で問題ないが、本格公開時には共通UI・各ツール・計算ロジック・データを分離するのが安全。
